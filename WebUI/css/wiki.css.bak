/* wiki.css - Direct BWiki Webpage Styles (Light Theme) */
.wiki-page {
  flex: 1 1 auto;
  display: flex;
  flex-direction: column;
  height: 100%;
  min-height: 0;
  padding: 0;
  margin: 0;
  overflow: hidden;
  background: #f8fafc;
}

.wiki-top-toolbar {
  flex: 0 0 auto;
  height: 54px;
  padding: 0 24px;
  background: rgba(255, 255, 255, 0.95);
  backdrop-filter: blur(10px);
  border-bottom: 1px solid var(--panel-border);
  display: flex;
  align-items: center;
  justify-content: space-between;
  box-shadow: 0 2px 8px rgba(30, 41, 59, 0.04);
  z-index: 10;
}

.wiki-toolbar-left {
  display: flex;
  align-items: center;
  gap: 16px;
}

.wiki-title-info {
  display: flex;
  align-items: center;
  gap: 10px;
}

.wiki-title-info h2 {
  margin: 0;
  font-size: 16px;
  font-weight: 700;
  color: var(--text-main);
  letter-spacing: -0.2px;
}

.wiki-url-badge {
  font-size: 11px;
  font-family: 'Fira Code', monospace;
  padding: 3px 8px;
  border-radius: 999px;
  background: #edf2f7;
  color: var(--text-muted);
  border: 1px solid #e2e8f0;
}

.wiki-toolbar-right {
  display: flex;
  align-items: center;
  gap: 10px;
}

.wiki-toolbar-right .secondary-btn,
.wiki-toolbar-right .primary-btn {
  padding: 6px 14px;
  font-size: 13px;
  display: inline-flex;
  align-items: center;
  gap: 6px;
  height: 34px;
}

.wiki-frame-container {
  flex: 1 1 auto;
  position: relative;
  width: 100%;
  height: calc(100% - 54px);
  min-height: 0;
  background: #ffffff;
  overflow: hidden;
}

.wiki-iframe {
  width: 100%;
  height: 100%;
  border: none;
  display: block;
  background: #ffffff;
}
