# 更新日志 / Changelog

本文件按版本记录 `AstralParty.Toys` 的功能变更。版本号与 GitHub 发布 tag 一一对应（如 `v0.2.0`），发布产物由 [release.yml](.github/workflows/release.yml) 在打 tag 时自动构建。

## [v0.5.0] - 2026-09-26

### 🩹 修复：点过「从 GitHub 更新」之后，模组页一直说「目录里是其它 version.dll」

加载器来源以前只跟**随程序内置的那一份**比哈希，于是「更新」这个**正常操作**反而触发了误报：
从 GitHub 拉到的发布包只要比本程序内置的新（这正是更新的目的），磁盘上的 `version.dll` 自然
和内置的不同 —— 界面就一直挂着「目录里面是其他 version.dll」，小白用户不敢动，老用户以为更新失败。

现在来源判定分三种，只有真正说不清的那一种才会报警：

- `bundle` —— 与内置那份逐字节一致（正常安装）；
- `installed` —— 与安装清单 `AstralParty_ModLoader\cesium-loader.json` 里记录的 `version.dll`
  哈希一致（**更新之后就是这一种**，属于本工具自己装的，一切照常）；
- 空 —— 清单里也没有、内置也不匹配，才认定「不是本工具装的（来源不明）」，此时才需要
  「允许覆盖其它 version.dll」/「强制卸载」。

### 🧩 模组页重做：版本信息放到最显眼的位置

这是个 mod 管理器，用户打开这一页多半就是想确认「装的是哪一版、要不要更新」，所以：

- 顶部常驻**版本三栏**：`已安装` / `随本程序内置` / `GitHub 最新`，字号最大、带状态色
  （绿 = 最新、金 = 可更新、红 = 文件不属于本工具、灰 = 未知），下面再给**一句人话结论**
  （例如「发现新版本：已装 v2.2.0 → 最新 v2.2.1，点「更新到 v2.2.1」」）；
- 主按钮随状态换角色：没装 → 「安装加载器」是主操作；装了 → 主操作自动变成「更新到 vX.X.X」；
- 模组列表改成**卡片**：名字 + 版本 + 「内置」/「⚠ 可操作游戏」标签 + 描述 + 事实行
  （作者 / SDK / 体积 / 目录 / 修改时间 / 依赖），右侧一个开关（已启用 / 已禁用）、`⚙` 配置、`🗑` 删除，
  并显示模组个数；
- 「检查更新」不再只弹一句 toast，而是把最新版本号回填到第三栏，结论也由前端统一算 ——
  同一个结论不再有两个说法要对齐。

### 🧹 去繁就简：不再猜版本号

- **删掉**了「从加载器日志尾部正则抠版本」和「拿配置里的 `sdkVersion` 顶替」两条兜底链。
  它们猜出来的数字会被当成权威值显示，对版本敏感的工具来说这比「不知道」更糟；
- 现在版本**只认安装清单**，读不到就如实显示「版本未知」，并提示「重新安装一次即可纳入版本管理」
  （老安装不会再被标上一个可能是错的版本号，界面也不再出现「（来自日志）」这类说明）；
- 「加载器设置」弹窗分层：日常项（启用加载器 / 基础倍速 / 控制台 / 转发日志 / Steam 绕过主开关）
  直接摊开，Steam 绕过细节、引导程序集、各种超时收进弹窗内的「高级选项」；
- 未安装加载器时，「加载器设置」与「游戏变速」整块禁用（此时 `doorstop_config.json` 还不存在，
  点了只会报错），不再让用户点了才知道不行；
- 修复一个会误导的输入范围：加载器设置里的「启动时基础倍速」以前接受 `0.1`，但加载器**不支持减速**、
  实际按 `1.0` 跑 —— 界面会写 0.5x 而游戏跑 1x。现在 UI 与提交校验都统一为 `1.0 ~ 100`。

### 🧱 实现说明

- `ModStatus` 增加 `LoaderOrigin`（`bundle` / `installed` / 空）与 `LoaderManaged`，删掉
  `InstalledVersionSource` 与三个从没用过的字段（`LatestVersion` / `UpdateAvailable` / `UpdateError`）；
- 安装 / 卸载的确认门改用 `LoaderManaged`：自己装的（含更新过的）一律不再要求勾选覆盖 / 强制卸载；
- 前端模组页样式独立到 `WebUI/css/mods.css`，`utilities.css` 里属于模组页的旧样式块（更新行、
  旧模组列表、弹窗表单、旧变速栏）全部清掉 —— 两个页面不再互相牵连。

### 📢 删掉首页的「游戏版本公告」

- 首页立绘上那张公告卡片（以及 `WebUI/data/game-version-announcement.json`）整体移除：
  它的数据是手写死档，没人维护就只会长期显示过期内容，还会盖住立绘；
- 后端里那份**从未被前端用过**的硬编码公告列表（`HomeDataService.GetAnnouncements()`，
  4 条编造的活动/维护公告）一并删除 —— 每次启动都白传一遍；
- 弹窗标题的默认文案从「公告详情」改成中性的「详情」。

### 🏷 版本号常驻界面：顶部栏 + 底部状态栏

版本信息以前只在设置页「关于」里有一行（还是硬编码的 `.NET 8 · Windows · WebView2`）。
现在做成一套统一的版本显示，**自己编译的构建同样看得出版本、提交、构建配置**：

- **详情页顶部栏**右侧常驻版本胶囊（`v0.4.11 · 18962b1`），大厅（无顶部栏）则在
  **底部状态栏**左侧显示（`AstralParty.Toys v0.4.11 · 18962b1 · 加载器 2.2.1`）；
- 两处都可点开「版本信息」：程序集版本、源码提交、构建配置（Debug/Release）、内置加载器、
  运行时与架构、操作系统、程序集文件时间、程序目录，并支持**一键复制**（用 WPF 剪贴板，
  比 `file://` 下的 `navigator.clipboard` 可靠）—— 报 issue 直接贴；
- 版本号来自程序集属性，提交号来自 .NET SDK 写进 `InformationalVersion` 的 `SourceRevisionId`：
  所以本地 `dotnet build` 的也能说清是哪一版、哪个提交；没有提交信息（例如从源码 zip 构建）
  或不带发布包时，界面写「本地构建」，而不是编一个版本号；
- 设置页「关于」改用同一份数据：应用版本 / 源码提交 / 运行环境（真实运行时与架构）/
  内置加载器，并加了「版本详情」入口。`HomeDataService` 里那个重复的 `appVersion` 字段删除，
  避免两个版本来源各说各话。

### ♿ 可用性修复（键盘 / 反馈 / 可读性）

- **焦点环回来了**：`base.css` 里 `button, input { outline: none }` 把键盘焦点可见性一起干掉了，
  全app没有任何一处能看出焦点在哪。现补上全局 `:focus-visible`（键盘 Tab 才出现，鼠标点击不出现），
  被裁剪的卡片/开关改用内描边；
- **弹窗焦点管理**：打开时焦点移入弹窗、关闭时还回原处、Tab 圈在弹窗内（配合新加的
  `role="dialog"` / `aria-modal` / `aria-labelledby`）—— 以前键盘用户打开弹窗后焦点还留在背后页面上；
- **耗时操作有反馈**：模组页的「安装 / 卸载 / 检查更新 / 更新到最新」按钮在等待宿主返回期间
  转圈 + 加锁（`.is-busy`，禁用但不灰化），宿主回推 `modStatus` / `modUpdateCheck` 或报错时解除，
  另有 2 分钟兜底 —— 以前点「更新到最新」下载十几秒内没有任何反馈，可以重复点击；
- 模组页小于 12px 的正文（版本栏标签与说明、模组事实行、高级区提示、配置项说明）统一提到 12px。

### 🧭 全app巡检修复：回放 / 变速器 / 维基 / 首页 / 设置

延续上面的模组页整改，把其余几页也过了一遍，修掉确认存在的缺陷：

**回放 / 游戏工具页**

- **内层技术页签不再连累外层标签**：外层「对局摘要 / 玩家 / …」和内层「数据统计 / 原始协议帧」
  用的是同一个 `.subnav-tab` 类，以前用全局选择器一起绑，点内层会把 `switchReplayTab(undefined)`、
  6 个外层标签的高亮全清掉。改成只绑 `#replaySubnav` 下、且带 `data-tab` 的标签；
- **换一局回放会清掉上一局的选中帧**：`renderReport` 开头复位 `selectedFrame = -1`，
  否则协议表里会有一行「假选中」高亮、右侧却是「请点击任意协议帧」的占位，自相矛盾；
- **回放库行内操作有忙碌态 + 防重入**：归档 / 放回 / 删除是文件 IO + 整表重扫，中间有空窗；
  以前点了没反应、再点一次会命中「游戏目录里没有这个回放」的分支弹误导性提示。现在操作中按钮
  置灰 + `aria-busy`，列表回来后解除（另有 15 秒兜底）；
- **从列表打开失效回放不再静默**：token 失效 / 文件被移走时，宿主以前直接 `return`、前端毫无反应；
  现在回一句「这个回放找不到了（可能已被移动或删除），点刷新列表重新扫描」；
- **「无法解析」徽章带上原因**：宿主本来就把 `error`（版本不兼容 / 文件损坏…）发过来了却被前端丢弃，
  现在挂到 `title` 上，鼠标一悬停就知道原因；
- **导出成功只报文件名**：以前 toast 里贴完整长路径，会把居中的胶囊撑出窗口；同时给 `.toast-container`
  加 `max-width` 与换行兜底；
- **席位横幅的两处逻辑修正**：① 横幅「开启自动托管」以前用 `capacity`(=10) 覆盖用户在设置页配的保留局数，
  现在改用真实的 `keepInGame`、并按 `autoMaintain` 把按钮切成「自动托管已开启（保留 N 局）」且禁用；
  ② 「稍后再说」的签名以前永久生效（席位再存满也不再提醒），现在席位腾空后自动清签名。

**维基页**

- **iframe 懒加载**：HTML 里不再写死 `src` —— 那会让这一页即使 `hidden` 也在启动时默默拉一整个第三方维基；
  改为首次进入维基页时才加载；
- **刷新不再卡成白屏**：旧逻辑先设 `about:blank` 再延时设回来，连点两次时第二次读到的已是 `about:blank`，
  框架就永久空白。现在直接重设记录下来的当前地址。

**首页 / 设置 / 通用**

- **启动按钮忙碌态**：游戏要好几秒才起得来、期间进程还查不到，以前只有一句 toast，容易「以为没点上又点一次」
  重复拉起。现在按钮短暂锁住 + 转圈（5 秒），并挡掉窗口期内的重复点击；
- **去掉立绘的假交互**：立绘 tooltip 以前写「点击角色互动 / 切换台词」，但该功能早已删干净、没有任何点击处理；
  移除误导的 tooltip 与手型光标；
- **品牌 Logo 可键盘操作**：顶部返回大厅的 `#brandLogoBtn` 是个 `div`，补上 `role="button"` /
  `tabindex="0"` 与 Enter/Space 处理；
- **手动输入的回放库目录也会保存**：以前只有点「浏览 / 打开」等按钮才落盘，直接敲进去会被静默丢弃，
  现补 `change` 监听；
- **自动托管标题不再写死「10 局」**：跟随实际保留局数显示（同一张卡片下面就能改成别的数）；
- **加载器 Steam 绕过状态回推口径统一**：在模组页保存加载器设置后回推给首页单选框时，改用
  `IsSteamBypassActive`（三个绕过开关任一为真都算「绕过」）—— 以前只看主开关，会出现首页显示
  「从 Steam 启动」但组队绕过仍开着、Steam 好友邀请进房静默失效；
- **变速器页「解析失败」徽章复活**：`configBroken` 是 payload 的兄弟字段、不在 `status` 里，
  以前读 `this.status?.configBroken` 永远是 undefined，配置解析失败也照显「已同步」。现在单独存取；
- **通用可读性 / 反馈**：通知胶囊补 `role="status"` / `aria-live="polite"`（读屏能念出来）；
  全局 `button:disabled` 统一置灰 + `not-allowed` 光标；变速器 / 回放 / 维基 / 设置几页里
  剩余小于 12px 的正文与提示统一提到 12px；给若干无障碍名缺失的开关 / 输入 / iframe 补 `aria-label` /
  `aria-labelledby` / `title`；`TryOpenExternal` 打不开系统浏览器时不再静默吞异常，改回一句提示。

### 🩹 修复：模组页「高级与排障」展开后几乎看不到内容

`.mods-body` 是个 `overflow-y:auto` 的纵向 flex 容器，里面的内容块（加载器面板 / 模组面板 /
「高级与排障」）默认 `flex-shrink:1`。当页面内容变高（这次顶部新增了「当前游戏」选择器又加了一截），
flex 布局会**先压扁可收缩的子块**去凑容器高度，而不是让容器出滚动条——「高级与排障」偏偏又是
`overflow:hidden`，一被压扁就把展开后的正文（游戏目录、安装/覆盖选项、SDK 依赖、原理说明等约 700px）
直接裁掉，只剩一条一百来像素的缝，于是「展开了也根本看不到什么」。

修复：给 `.mods-body > *` 设 `flex-shrink:0`，内容块保持自然高度、`.mods-body` 正常出现滚动条，
展开后能完整看到并滚动浏览。

### 🎮 新功能：多游戏目录管理 + 系统搜索

以前工具只记一个游戏目录，而且模组页与变速器页各存各的（`modloader-state.json` /
`speedhack-state.json`），同时装了国服 / 国际服 / TapTap 端就得反复手动改路径。现在：

- **设置页新增「游戏目录（多个位置）」卡片**：可保存任意多个游戏位置，每条带**区服标签**
  （国服 / 国际服 / TapTap / 自定义，按 exe 名与路径自动识别、可改）、可改名、可「设为当前」、
  可「打开目录」、可移除；失效目录（已删 / 非标准 Unity 结构）会标红提示；
- **模组页顶部新增「当前游戏」下拉**：切换后，安装加载器、变速、导入模组等操作都作用于选中的这个游戏；
  切换会同步写进两个管理器的状态文件，模组页与变速器页看到的始终是同一个游戏；
- **系统搜索**：
  - **快速扫描**（秒级）——顺着 Steam 库（含多盘 `libraryfolders.vdf`）+ 注册表（卸载项 / `App Paths`）
    + 常见安装位置（各盘 `Program Files`、`TapTap`、`Games` 目录）找 `AstralParty*.exe`；
  - **深度扫描**——快扫找不到时，可选一个盘符全盘遍历（后台进行、带进度、可随时取消）；
  - 每个候选都标注**是否为标准 Unity 目录结构**（存在 `<exe名>_Data\` 且同目录有 `UnityPlayer.dll`）、
    命中来源与区服，一键「添加」；
- **首启自动迁移**：旧的单目录记忆 + Steam 自动检测结果会自动播种成档案，老用户设置不丢；
- 变速器页 / 模组页原有的「自动检测 / 选择…」按钮改为同时写入档案并设为当前，不再和多位置管理脱节；
- **游戏主程序检测改为通配** `AstralParty*.exe`：以前 `ContainsGameExe` 硬编码只认
  `astralparty.exe` / `astralparty_cn.exe` 两个确切名字——于是手动选了 TapTap 端 / 其它渠道
  （exe 叫别的名字）的目录时，「浏览添加」能过（那步本来就是通配），但后续安装 / 变速 / 状态检测
  却按硬编码名字报「目录里没有 AstralParty.exe / AstralParty_CN.exe」。现在全链路统一用
  `AstralParty*.exe` 通配（加载器是按 exe 旁边的位置加载的，跟 exe 具体叫什么无关），相关提示同步改口；
- **`Post` 改为线程安全**（深扫进度回调来自后台线程，统一 marshal 回 UI 线程再访问 WebView2）。

### 🎨 设置页重新布局：铺满窗口、控件占满容器

- 去掉 `.settings-grid` 的 `max-width: 1100px` 硬顶（窗口最大化时右侧一大片留白），改为
  自适应铺满、最宽 1560px 居中；卡片按可用宽度自动排列（宽屏三列、窄屏回落）；
- 「多位置管理」作为本页最大的功能块**独占整行**，内部再左右分栏（左档案列表 / 右操作 + 系统搜索），
  把横向空间用满；档案行、扫描结果、输入框均改为占满各自容器；
- 当前游戏档案用品牌粉高亮 + 左侧色条，卡片标题右侧显示位置计数；正文说明限制到约 78ch 保证可读性。

### 📝 说明：「回放库 → 库目录」补充"这也是配置文件夹"

设置页「回放库」的库目录以前只说是回放归档位置，容易让人以为配置在别处。现在在库目录下方补了一句：
默认目录 `文档\AstralPartyReplays` 同时也是工具箱的**配置文件夹**（游戏库 / 变速器 / 加载器状态 / 回放库设置都在这），
并说明把库目录改到别处只会移动回放归档、配置文件仍留在默认目录。

### 🩹 修复：窗口最大化后设置页「关于」卡片错位 / 排版不均

`.settings-grid` 原先用 `auto-fit` + `minmax(340px,1fr)`，最大化时会撑出 4 列，但小卡片只有 3 张、
又夹着一张整行的「游戏目录」，于是出现「动画偏好孤零零占一列、右边空一大片」「关于飘在中间列」
这类错位。改为**显式响应式列**（宽屏 3 列 / 中屏 2 列 / 窄屏 1 列），并让「游戏目录」`order:-1` 置顶独占整行，
后面三张小卡（动画偏好 / 回放库 / 关于）正好填满下一行，任何宽度下都排得整齐。

### 📜 新功能：程序内查看更新日志（工具箱 + 加载器）

- 「关于」卡片新增「📜 更新日志」按钮：仓库根的 `CHANGELOG.md` 编译进程序集，点开由宿主读出、
  在弹窗里以可读排版（版本标题 / 分类小标题 / 列表 / 加粗 / 行内代码）渲染，不必再去 GitHub 翻；
- 弹窗分**两个 Tab**：`🧰 工具箱`（本程序的 CHANGELOG）/ `🧩 加载器`（CesiumLoader 的更新说明）。
  加载器日志来源：**CI 打包时内嵌**该版本 Release 说明（离线即时），内嵌为空 / 点「↻ 拉取最新」时
  **运行时去 GitHub 拉**最新 Release 说明兜底；模组页「加载器」面板也加了「📜 加载器更新日志」按钮直达该 Tab；
- **大厅「公告位」**回归：更新到新版本后，大厅左上角出现一张紧凑更新卡（版本号 + 本次要点 + 「查看完整更新日志」）。
  点「知道了」记住这一版不再提示（存 `localStorage`），**下次更新到更高版本时再自动出现一次**。

### 🧭 大厅右侧菜单去掉「维基」入口（顶部菜单栏仍保留）

大厅右侧的功能卡精简为 变速器 / 回放 / 模组 三张；「维基」不再占大厅入口。需要看维基时，
从顶部菜单栏的「📖 维基攻略」进入即可（维基页本身与懒加载逻辑不变）。

### 🎮 启动游戏：大厅「启动版本」下拉 + 不再拦截再次启动 + 启动方式冷却

- **大厅新增「启动版本」下拉**：直接从游戏库里选要启动的版本（国服 / 国际服 / TapTap / 自定义），
  选中即设为当前游戏；下面「从 Steam 启动 / 绕过 Steam 启动」两种方式都对选中的这个版本生效。
- **启动方式不再自作聪明**：选了哪个版本、哪种启动方式就照做，不再按区服猜"这个版本在不在 Steam 上"、
  也不弹「Steam 上没有对应版本」之类的提示。目前「通过 Steam 启动」只有固定 appid 一条路，
  「绕过 Steam 启动」= 直接启动选中版本的主程序 —— 组合是否合理由用户自己把握。
- **不再因「游戏已在运行」拦截**：保存了多个游戏位置后，用户可能想在已开一个的情况下再启动另一个
  （例如国服 + 国际服），或第一次没起来想立刻重试。是否真能起第二个实例交给游戏 / Steam 自己判断；
- **启动后给「启动方式」加冷却**：游戏在启动早期才读加载器的 Steam 绕过配置。若点完启动手一快、
  立刻切到另一种方式，切换会改写那份配置、让刚起的这一局"串味"（从 Steam 起的却被绕过，反之亦然）。
  现在启动后单选框会短暂上锁（约 6 秒）禁止切换，干净地避开这个竞态。


## [v0.4.10] - 2026-09-25

### 🎥 内置自由相机整体重做：滚轮缩放（视角完全不变）

内置 CesiumLoader 加载器 / SDK / 内置 mod `2.2.0 → 2.2.1`，重点是**自由相机重写** ——
旧版那套「`F1` 开关俯瞰视角 + 滚轮调高度」已经全部删掉：

- 进游戏**不再接管**任何东西，也不需要开关：**鼠标滚轮沿当前视线前后移动** —— 只写相机位置，
  朝向 / 俯角 / FOV 一律不写，所以是「视角不变，只是远近变了」；向前滚 = 拉近，向后滚 = 拉远；
- `F1` 恢复**原来的视角**（`F2` 把缩放归零），两个键都能在模组页该 mod 的 `⚙` 里改；
  滚回原处、切场景、换相机、卸载 mod 都会把相机完整交还游戏；
- 旧的 `config.json` **首次启动自动迁移**：清掉 `mode` / `height` / `pitch` / `heightStep` 等旧键，
  写入新 schema（`zoomStep` / `zoomInMax` / `zoomOutMax` / `invertWheel` / `smooth*` / `restoreKey` / `resetKey`），
  原来的 `toggleKey` 会接成 `restoreKey`，不用重设；
- SDK 侧新增 `FreeCameraMath.ApplyScrollToZoom / SmoothTowards / DollyPosition / IsSettled`
  （滚轮步进、限位与阻尼共用同一套数学），加载器侧 334 个单元测试全部通过。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK / 内置 mod `2.2.0 → 2.2.1`，内嵌的 `doorstop_config.json` 模板里
  `sdkVersion` 一并同步；
- 「安装 / 修复加载器」用的内嵌资源已经是 2.2.1；也可以直接点模组页的「**⬇ 从 GitHub 更新**」，
  它拉的是 CesiumLoader 最新 Release，和本版内嵌的是同一份。

### 💬 更新时注意

- 更新**不会覆盖**你的 `doorstop_config.json`（Steam 绕过开关、变速倍率原样保留），只会把里面的
  `sdkVersion` 抬到 `2.2.1` —— 这一步是必需的：加载器用它校验每个 mod 声明的 `SdkVersion`，
  没抬上去的话新版内置 mod 会被判成「要求更高的 SDK 版本」而**全部拒绝加载**（游戏内热键全失灵）；
- 安装 / 更新前请**完全退出游戏**（`version.dll` 被占用时无法覆盖），更新完重启游戏生效。

### 📝 文档

- README 的模组章节同步新行为（自由相机 = 滚轮缩放、内嵌版本 2.2.1）。

## [v0.4.9] - 2026-09-22

### 🩹 修复：「从 Steam 启动」没有真正恢复 Steam 大厅联机

上一版（0.4.8）在选「从 Steam 启动」时只把 `steamBypassEnabled` 一个键写成 `false`，**这是不够的**：
加载器的 hook 都是**启动时无条件安装、不检测 Steam 是否在运行**的（加载器 `docs/steam-bypass.md` 第 6 节第 4 条），
而且各自只受自己的键控制 ——

- `steamBypassEnabled` → 阶段1：拦 `SteamManager.Awake`（"非 Steam 客户端启动就退出"）与 `SteamAPI_RestartAppIfNecessary`；
- `steamBypassMatchmaking` → 阶段2/3/方案B：`CreateLobbyAsync` / `JoinLobbyAsync` 换成已完成的空 Task（`steamLobbyId` 恒为 0）；
- `steamBypassLobbyQuery` → 阶段5：`LobbyQuery.RequestAsync` 换成"结果为空"（源码注释明写"只受它自己控制"）。

所以只关主开关时，**从 Steam 启动仍然不会创建真大厅、房间列表照样返回空**，Steam 好友邀请 / 大厅分享进房依旧是坏的。
现在这一套**三个键一起开、一起关**：

- 选「绕过 Steam 启动」→ 三个键全写 `true`；选「从 Steam 启动」→ 三个键全写 `false`（等于完全回到原版行为）；
- 首页单选框的状态判定改成"**三个里任一个还开着**就算绕过模式"：混合状态（比如手动只关了主开关）
  不会再被显示成「从 Steam 启动」，点一下「从 Steam 启动」就能把整套收拾干净；
- 其余几个键不用动：`steamBypassRestartCheck` 只在主开关为真时有意义，`steamBypassLobbyHasValue` 与「方案B」
  都挂在大厅匹配那个分支里，`steamBypassLobbyMethods` 无论如何都必须保持 `false`。

### 💬 提示

- 没装加载器时选「绕过 Steam 启动」会明确提示"还没安装加载器 —— 缺了它的 Steam 绕过，游戏会在启动早期自己退出，
  可在「模组」页一键安装，或改选「从 Steam 启动」"（与"装了但绕过是关的"分成两种措辞，都只提醒不拦截）；
- 「加载器设置」里 Steam 绕过那一组的标题补了一句：首页的单选框会把前三个开关作为一套一起开 / 关。

### 🧪 测试

- 「首页启动方式 → 加载器配置」的用例重写为整套语义：三个键一起关（文件长度只 +3、模板注释与其它键原样）、
  幂等、混合状态归一、老配置里没有这些键时不写盘、**不碰注释里的同名字面量**、UTF-8 BOM 保留；
  新增 `IsSteamBypassActive` 判定用例（三个里任一为真即为绕过模式）。

## [v0.4.8] - 2026-09-22

### 🎛 启动方式可选：从 Steam 启动 / 绕过 Steam 启动

主界面「启动游戏」按钮下方新增一组单选框，二选一（默认「从 Steam 启动」）：

- **从 Steam 启动**：交给 `steam://rungameid/2622000`，DRM / 云存档 / 更新检查都走 Steam；
  Steam 没装或协议没注册时，仍然退回直接启动游戏主程序；
- **绕过 Steam 启动**：直接拉起游戏主程序，完全不经过 Steam。游戏本体发现"非 Steam 客户端"会在启动早期
  自己退出（`[SteamManager] 非Steam客户端启动, 退出游戏`），所以这条路依赖加载器的 Steam 绕过 ——
  而这两者本来就是**同一个配置**：首页切换会写进 `doorstop_config.json` 的 `steamBypassEnabled`，
  在「加载器设置」里改这个开关也会回推到首页；进界面时以配置里的当前值为准（没装加载器则用本地记住的选择）；
- 写入加载器配置时**只改这一个字面量**：注释、其它键、缩进原样保留（不走 `SaveLoaderConfig` 的白名单重写），
  写完先自检（仍能解析 + 读回来就是目标值）才落盘；
- 绕过启动时如果 Steam 没在运行、加载器的绕过又没生效，会提示一句"游戏可能自己退出" —— 只提醒不拦截。

### 📝 文档

- README 的模组章节补上加载器本体说明：CesiumLoader 是本项目的**配套加载器**（独立仓库、Doorstop 式
  `version.dll` 代理 + MinHook 原生 hook + HybridCLR 就绪后 `Assembly.Load` 拉起托管 mod），
  因此能在 AOT 的 `SteamManager.Awake` 之前拦下"非 Steam 客户端启动就退出游戏"；同时列出 Steam 绕过这组
  开关与新的启动方式；
- 修正 README 里过期的测试用例数，`docs/replay-format.md` 的测试表补上 `ModManagerTests` 一行。

### 🧪 测试

- 新增 6 个用例覆盖「首页启动方式 → 加载器配置」的写入：只动一个字面量（文件长度只 +1、模板注释仍在）、
  幂等、配置缺失时不凭空建文件、不碰注释里的同名字面量、UTF-8 BOM 保留。

## [v0.4.7] - 2026-09-22

### 🚀 不装 Steam 也能启动游戏（内置加载器 / SDK 2.1.7 → 2.2.0）

内置 CesiumLoader 升级到 **2.2.0**，加载器新增 **Steam 绕过**：国服客户端在 Steam 没运行时启动，
原本会打印「`[ERROR] [SteamManager] 非Steam客户端启动, 退出游戏`」并直接退出，现在这条校验被拦下，游戏正常进主界面。

- **不装 Steam 也能启动**：拦掉 AOT 的 `SteamManager.Awake`。这**只能由加载器做**——托管 mod 是在 HybridCLR
  就绪之后才被加载的，那时 `Awake` 早就执行完、游戏已经退出了；
- **修「点创建房间 / 加入房间没反应」**：无 Steam 时 `CreateLobbyAsync` 自己抛 `NullReferenceException`，
  这个 async 异常把整个建房流程吃掉，现在改成直接返回"已完成的空结果"；
- **修退房 / 被踢时的报错**：`LobbyQuery.RequestAsync` 返回空列表，退房清理、返回房间列表页恢复正常
  （以前会连抛 6 条 NRE，`await` 之后的代码全被跳过）；
- 这套开关都在 **模组页 →「⚙ 加载器设置」→ Steam 绕过** 里可视化开关（`steamBypass*`），默认全开，改完重启游戏生效。
  其中「方案B 备用安全网」标注了⚠：实测开启会导致游戏启动崩溃（`0xC0000005`），**保持关闭**。

### 🔧 升级链路修好了

- **内嵌安装现在也写 `cesium-loader.json`**。以前只有「从 GitHub 下载发布包」这条路会写它，
  所以用内置资源安装的用户**读不出「已安装版本」**，界面上永远看不到"随包 2.2.0 / 你装的是 2.1.7"，
  想升级也不知道该不该升。现在两条安装路径写同一份清单（版本 + 每个文件的 SHA256）。
- **修 CI 同步漏一个文件、会让内置 mod 全部失效**：发布流水线同步加载器包时没有同步 `doorstop_config.json`，
  而「把用户已装配置的 `sdkVersion` 抬到随包版本」正是读这个内嵌模板的 —— 模板停在旧 SDK 号，
  用户配置也就停在旧的，而新版内置 mod 声明的是 `SdkVersion = 2.2.0`，加载器会判 `SdkVersionTooNew`
  把内置 mod **全部拒绝加载**。现在模板一并同步。
- **老配置自动补齐 Steam 绕过开关**。新加载器读旧配置时，缺失的键本来就用编译进去的默认值（= 全开），
  所以**功能上从来不受影响**；但安装 / 更新现在还会把这 7 个开关**补进**你已有的配置
  （只追加缺失的键，注释和你已有的值一字不改）。两个好处：配置自文档化；
  以及防止**旧版 Toys** 保存「加载器设置」时把它们删掉 —— 那会把你特意关掉的绕过又打开。
- **配置解析容忍 UTF-8 BOM**：记事本保存 UTF-8 会加 BOM，带 BOM 的配置以前会让版本同步静默失效。
  C++ 加载器侧本来就剥 BOM，现在 C# 侧行为与它一致。

### 🔍 「已装版本」读得出来了（老安装也能）+ 加了降级闸门

- **老安装的版本回读**：0.4.7 之前装的加载器没有安装清单，界面上的「已装版本」一直是 `—`，
  只能看到含糊的「文件不一致」。现在按三层来源推断并**标注来源**：安装清单（权威）→
  加载器日志里它自报的那行 `CesiumLoader loader v2.1.7 / SDK v…`（显示「来自日志」）→
  配置里的 `sdkVersion`（显示「来自配置」）。分层是有原因的：日志里那行只有**成功走到 mod 加载阶段**
  才会写 —— 我拿真机历史日志验证过，那几个停在 `GameAssembly.dll 超时` 的运行里根本没有这一行，
  而配置是装完就在的。
- **降级闸门**：已装版本比随包 / 发布包更新时，安装与更新都**默认拒绝**并说明原因，
  避免将来"用旧版 Toys 把更新过的加载器静默降级"。确实要降级，需显式勾选新增的
  「允许降级安装较旧的加载器」。判断只采信安装清单与日志自报的版本，
  **不采信配置里的 `sdkVersion`**（那是另一个字段，而且用户会手改它）。

### 🔧 内置组件

- 内置 CesiumLoader 加载器 / SDK / 内置 mod `2.1.7 → 2.2.0`（`sdkVersion` 同步，加载器配置模板新增 Steam 绕过开关）；
- 加载器侧新增设计文档《Steam 无进程绕过》记录四阶段原理、配置全表、实测矩阵与回退方法；
- Toys 侧 102 个单元测试全部通过（新增 6 个：开关补齐的保留用户已有值 / 保留注释 / 幂等，
  以及版本回读三层来源与降级闸门）。

## [v0.4.6] - 2026-09-21

### ⌨️ 热键可以在界面里改了（支持鼠标侧键）

模组页每个 mod 的 `⚙` 配置里，字段名以 `Key` 结尾的（`toggleKey` / `speedUpKey` / `speedDownKey` / `resetKey` …）
不再是一行要你手打 `KeyCode` 名字的文本框，而是一个**按键按钮**：点一下，再按下想绑的键就完事。

- 键盘任意键都能绑（`A`~`Z`、`F1`~`F12`、小键盘 `+`/`-`、`Delete`、方向键…）；
- **鼠标左/右/中键与侧键**也支持（侧键在 Unity 里叫 `Mouse3` = 后退、`Mouse4` = 前进）——
  这两个键以前**根本读不到**：SDK 的 Win32 兜底后端键位表漏了它们，设了等于没设，而且不报任何错；
- 捕获期间 `Esc` 取消；右边的 `×` 清空该字段 = 退回 mod 内置默认键；
- 认不出来的键（媒体键等）只提示、不写入，不会偷偷存一个 mod 读不懂的名字；
- 侧键在 WebView 里默认是"浏览器后退/前进"，捕获时已把默认行为压掉，不会跳页。

键位依然就是普通字符串存在 mod 的 `config.json` 里，所以手改 JSON / 高级编辑一样能用。
改完**重启游戏**生效（mod 只在初始化时读一次配置）。

### 🖱️ 自由相机：高度改由鼠标滚轮调

俯瞰视角（`F1` 打开）下，**鼠标滚轮直接调高度**：向前滚 = 抬高，向后滚 = 降低，每格 `heightStep`（默认 10 米）。
原来的 `Ctrl` + `=` / `Ctrl` + `-` 保留同效，`heightStep` 对两者都生效。

顺带修了一个隐患：滚轮原先只有**一个**取值来源（`UnityEngine.Input.mouseScrollDelta` 的反射），
而 Win32 兜底后端**根本没有滚轮实现**（直接返回 `0`）—— 也就是说那条路一旦失灵，滚轮就是彻底静默失效。
现在补了 Unity 轴 `Mouse ScrollWheel` 的兜底，并把轴的 `0.1`/格统一折算成 `1`/格，
让自由相机的视野角 / 距离 / 高度三处共用同一套"每格多少"的步进。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK / 内置 mod `2.1.6 → 2.1.7`：
  - 按键捕获所需的鼠标侧键 / 小键盘键位映射（SDK 的 Win32 兜底后端）；
  - 滚轮调高度（`FreeCameraMath.ApplyScrollToHeight`）与滚轮取值的轴兜底；
  - 加载器配置模板的 `sdkVersion` 一并同步到 `2.1.7`；
  - 加载器侧 324 个单元测试全部通过。

## [v0.4.5] - 2026-09-21

### ✨ `Delete` 变成 `1.0x` 与"刚才的倍率"之间的双向开关

`Delete` 原本只是"设为 `1.0x`"：从 `2.0x` 按一下就回到原速，想再变速只能重新按 `Alt` + `=`。
现在它是**开关** —— 再按一次 `Delete` 就回到刚才的 `2.0x`：

- 按 `Delete` 时记下**当时的引擎倍率**（也就是被压回 `1.0x` 之前的值），再按一次切回去；
- 记忆**只活在本次游戏进程里**：重启游戏不会凭空回到旧倍率，也**不写回 `config.json`**
  （你保存的变速设置不会被 `Delete` 改掉）；
- 已经在 `1.0x` 且本次运行还没有任何记忆时，只提示"已是 `1.0x`"，不会乱跳；
- 想恢复旧的"永远设为 `1.0x`"：给内置变速 mod 的配置加一行 `"toggleRestoresSpeed": false`
  （`AstralParty_ModLoader\mods\SpeedHackMod\config.json`，不写这个键时默认开启）。

### 🐛 修复：从旧版升级后，内置 mod 全都不生效

加载器会用 `doorstop_config.json` 里的 `sdkVersion` 校验每个 mod 声明的 `SdkVersion`，**声明更高就直接拒绝加载该 mod**。
而本工具升级时为了不重置用户的变速倍率 / 开关，**不会覆盖**已经存在的 `doorstop_config.json`，
却会把内置 mod（行为日志 / 自由相机 / 变速）覆盖成随包版本 —— 于是从旧版升上来的玩家，
配置里的 SDK 号还停在旧号，新版内置 mod 会被加载器**逐个拒绝加载**：游戏里热键全失效，
加载器日志里每个内置 mod 都是一行"要求更高的 SDK 版本"。

修复：安装 / 更新时把配置里的 `sdkVersion` **抬到随包版本**，而且只动这一个字段：

- **只升不降** —— 配置里的号比随包版本还高（比如你自己装了更新的加载器）时保持原样；
- 其余设置与注释**原样保留**（不是整个文件重写，变速倍率 / 开关都不受影响）；
- 配置里本来就**没写**该字段时也不添加（加载器会用它自己编译进去的默认值）。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK / 内置 mod `2.1.5 → 2.1.6`：
  - `Delete` 双向切换（上面那条），并新增 `toggleRestoresSpeed` 开关；
  - 加载器配置模板的 `sdkVersion` 一并同步到 `2.1.6`（配合上面的升级修复）；
  - 加载器侧 313 个单元测试全部通过。

## [v0.4.4] - 2026-09-21

### 🐛 修复：游戏内变速热键"按了没反应"

上一版把变速接到了 `Delete` / `Alt` + `=` / `Alt` + `-` 上，但实际按下去**毫无反应，控制台也没有任何输出**。

根因：**本作的热更程序集（HybridCLR 加载的 SDK / mod）在运行时无法 P/Invoke** ——
任何 `[DllImport]` 调用都会抛 `NotSupportManaged2NativeFunctionMethod`（IL2CPP 只为构建期已知的
程序集生成 managed→native thunk），连 kernel32 的 `GetModuleHandleW` 都调不通。
于是 SDK 的 `SpeedHack` 永远调不到加载器的 `ap_speed_*` 导出，`IsAvailable` 恒为 `false`，
`SpeedHackMod` 在 `Main()` 里就直接退出了（日志里只有一行"变速引擎不可用"）。

修复方式：加载器与 mod 之间改用**变速控制文件通道**（日志转发本来就是同一思路）：

- 加载器（**2.1.5**）常驻线程每 100ms 检查 `AstralParty_ModLoader\speed\request.txt`，
  内容变化且合法就应用倍率，并回写状态文件 `state.txt`（`version/speed/base/active/hooks`）；
- SDK 的 `SpeedHack` 改成读写这两个文件，**公共 API 不变**（`IsAvailable` / `Speed` /
  `SetSpeed` / `Reset` / `StepSpeed` / `ClampSpeed`），新增 `BaseSpeed` / `Describe()` 便于排查；
- `SpeedHackMod` 的行为与配置完全不变：`Delete` 设为 `1.0x`、`Alt` + `=` / `Alt` + `-` 实时调倍率、
  调完写回 `config.json`；请求落盘后约 0.1 秒生效。

> 需要内置加载器 **≥ 2.1.5**（这一版已内嵌）。加载器设置里新增「允许 mod 热键变速」
> （`speedControlEnabled`），关掉后基础倍速仍然生效，但没有文件通道、热键不再可用。

### 🐛 修复：切回 `1.0x` 时画面冻结约十秒

按 `Delete` 或一路 `Alt` + `-` 把倍率调回 `1.0x` 时，画面会僵住（**声音还在**），约十秒后自行恢复。

根因：原生变速引擎的 4 个时间 hook 里有一条"1.0 就图省事直接返回真实时间"的快捷路径，
绕过了切速时用来保持时间连续的 `offset`。于是从 2x 切回 `1.0x` 的瞬间游戏看到的时钟会**倒退**，
倒退量正好等于加速期间攒下的虚拟时间 —— 主线程遇到时间倒流就卡住，等亏空被追平才恢复
（之所以稳定表现为"十秒"，是因为热键在启动 10 秒后就绪，而加载器基准倍率是 2x）。

修复：`1.0` 照样走虚拟时间公式（`speed == 1.0` 时用整数平移，精确且不跳变），不再有捷径。
冒烟测试新增「时间连续性」小节，直接读被 hook 的 `GetTickCount64` 断言切回 `1.0x` 时
时钟既不倒退也不跳变（修复前该断言失败，实测倒退 800ms+，且随加速时间线性增长）。

### 🚫 变更：倍率下限 `1.0` —— 不允许减速

所有低于 `1.0` 的变速请求会被**三层**拦截，把 `config.json` 的 `minSpeed` 改成 `0.2` 也没用：
加载器的请求解析（`parse_speed`）、加载器的 `speedhack_set_speed`、SDK 的
`SetSpeed` / `ClampSpeed` / `StepSpeed`。`doorstop_config.json` 的 `speedhackBaseSpeed`
同样不允许小于 `1.0`（非法值退回 `1.0` 并记日志）。

- `Delete` 仍然是"设为 `1.0x`"（引擎保持开启，不卸载 hook）；
- `Alt` + `-` 到 `1.0x` 就到底，再按只提示"已是极限"；
- 状态文件里若出现 `< 1.0` 的旧值（旧加载器或外部工具写的），SDK 也按 `1.0` 处理；
- 本工具写入加载器配置（`speedhackBaseSpeed`，即"每次进入游戏自动 N 倍速"）的校验同步收紧到
  `[1,100]`，填更小的值会直接报错，不再写出一个注定被加载器忽略的数字。

### ⚠️ 新增：旧版变速器页面的互斥警告

「游戏工具」页装的是**旧版独立变速器**（`speedhack-rs`），它和模组加载器用的是同一个
`version.dll`，两者**只能装一个**（同时装会互相覆盖，游戏只会加载其中一个）。
该页顶部现在有一块醒目的警告框，把两条路讲清楚：

- **只想要变速**（不装任何 mod）→ 留在本页照常安装；
- **想要新功能**（模组管理、自由相机、行为日志、权限声明、游戏内热键变速……**也包括变速本身**）
  → 去「模组」页面，警告框里自带「🧩 去模组页面」按钮。

同时把模组页的变速输入框与保存校验对齐到加载器的硬性下限（最小 `1.0`，提示"不支持减速"）。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK `2.1.3 → 2.1.5`：
  - **2.1.4**：打包修复 —— CI 产出的压缩包之前漏了 `SpeedHackMod`（内置 mod 清单现在有唯一来源）；
  - **2.1.5**：新增变速控制文件通道（上面的修复），新增 `speedControlEnabled` 开关与 `CESIUM_SPEED_DIR`；
    文档补上"热更程序集无法 P/Invoke"这条硬约束。
- SDK 单元测试 `276 → 313`，新增控制文件通道用例（可用性判定、原子写盘、区域无关小数、
  非法倍率不落盘、`NaN`/`±Inf` 与 `< 1.0` 一律拒绝）与时间连续性用例。

## [v0.4.3] - 2026-09-21

### ✨ 新增内置 mod：变速（SpeedHackMod）

变速引擎本来就是加载器内置能力（`doorstop_config.json` 的 `speedhackBaseSpeed`），
但改倍率要重启游戏。这一版把它接到了键盘上，**游戏里随时调**：

- `Delete`：开关变速（开 = 上次的倍率，关 = 回到 `1.0x`）；
- `Alt` + `=`（即 `+`，小键盘 `+` 也行）：**加速**；`Alt` + `-`：**减速**；
  每次 `speedStep`（默认 0.5，按住不放会连续调）；
- **只读键盘**：不接管鼠标、不做输入独占、不拦截游戏自己的按键，不影响正常游玩；
- 屏幕上弹当前倍率（`变速: 2.5x`），到上下限（默认 `0.1x` ~ `10x`）时提示"已是极限"；
- 调完停手约 1.2 秒自动写回 `mods\SpeedHackMod\config.json`，下次启动沿用；
- **不覆盖**你在 doorstop / 模组页面设的基准倍率：启动时引擎已是 2.0x 就当作"已开启 2.0x"，
  只有按 `Delete` 或 `Alt` + `+`/`-` 之后才由 mod 接管；
- 倍率/热键/范围全都能在 `config.json` 里改（`speedStep` / `minSpeed` / `maxSpeed` /
  `toggleKey` / `speedUpKey` / `speedDownKey` / `useNumpadKeys` / `rememberSpeed` / `notify`）。

> ⚠️ 变速影响游戏感知的所有时间（动画/回合/**网络超时**），倍率别调太高（建议 ≤3x）。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK `2.1.2 → 2.1.3`：
  新增 `SpeedHack.StepSpeed` / `ClampSpeed`（热键算倍率用的纯函数，带单元测试），
  并修掉 `SetSpeed` 对 `NaN` 的漏检（`NaN` 会让游戏虚拟时间彻底坏掉）。

### 📋 其它

- 模组页勾选项文案更新为「同时安装内置 mod（行为日志 + 自由相机 + 变速，含 sidecar 元数据）」。

## [v0.4.2] - 2026-09-21

### ✨ 自由相机可在游戏内实时调高度

- 俯瞰视角开着的时候，**`Ctrl` + `=`（即 `+`，小键盘 `+` 也行）抬高相机、`Ctrl` + `-` 降低**，
  每按一次 `heightStep` 米（默认 10，改 `mods\FreeCameraMod\config.json` 即可调整）；
- **立刻生效，不必重启游戏**：抬高的视角每帧重算位姿，固定俯瞰模式在按键时重算一次；
- 屏幕上会弹出当前高度，到 5 ~ 2000 米的上下限时提示"已到极限"；
- **只读键盘、不抢鼠标**：不接管鼠标、不拦截游戏自己的按键，所以不会影响正常游玩；
- 调完停手约 1.2 秒自动写回 `config.json`（退出视角时也会补写），下次启动继续用这个高度。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK `2.1.1 → 2.1.2`：新增 `FreeCameraMath.ApplyHeightStep`（高度步进 + 夹紧的纯函数，
  带单元测试），自由相机的高度热键就是走它。

## [v0.4.1] - 2026-09-21

### 🐛 修复

- **视角抬高后游戏操作失效**：自由相机原来的固定俯瞰（`preset`）每帧把相机位姿写死，游戏自己的鼠标转视角 /
  键盘平移因此完全没有反应（开着动不了、关掉才恢复）。新增**跟随抬高**模式 `mode: "raise"` 并设为默认：
  每帧以"游戏写下的相机位姿"为基准，只把相机抬到配置高度俯视，水平朝向与瞄准点跟着游戏走 ——
  既能从高处看棋盘，游戏自带操作照常可用。想要完全固定的机位仍可选 `mode: "preset"`。
- **mod 配置改了游戏里不生效**：Toys 的配置表单读写的是 `AstralParty_ModLoader\configs\{mod}.json`，
  而 mod（SDK 的 `ModConfig`）读的是**自己 dll 旁边的** `mods\{ModId}\config.json`，两边根本不是同一个文件。
  现在统一读写 `mods\{ModId}\config.json`；旧的 `configs\` 配置仍能读出来，保存或"高级编辑"时会自动迁移过去。
- **偶发画面变灰/全黑（HUD 与声音还在）**：退出自由相机时会"完整还原"相机快照，而快照可能采自菜单/切场景
  那一刻的**失活过渡相机**（`enabled=false`、停在极高处）—— 把 `enabled=false` 还原回去就等于把游戏正在
  渲染的相机禁用了。SDK 的 `RestoreState` 改为**默认不还原 `enabled`**（需要时显式传 `restoreEnabled: true`），
  自由相机同时加了接管前就绪校验：占位/过渡相机或瞄准点明显不合理时**拒绝接管**。

### 🔧 内置组件升级

- 内置 CesiumLoader 加载器 / SDK `2.1.0 → 2.1.1`（相机快照还原语义 + 主相机解析择优启用相机 + 自由相机跟随抬高）；
- 内置自由相机 mod 同步升级，配置文件默认 `mode` 变为 `raise`。

## [v0.4.0] - 2026-09-20

### ✨ 内置 CesiumLoader 升级到 2.1.0（加载器 + SDK + 内置 mod）

内嵌加载器组件整体升级到 CesiumLoader 2.1.0，工具开箱即用的 mod 能力一次性补齐：

- **SDK 2.1.0**：新增相机接管（`CameraService` / 相机状态快照与还原 / Cinemachine 接入）、输入与光标（`InputService`）、
  UI 登记（`UiService`）、场景与协程（`SceneService` / `CoroutineService`）、运行时反射（`RuntimeAssemblyService`）、
  诊断转储（`SdkDiagnostics.Dump`）；
- **引擎调用隔离**：SDK 全部 Unity 引擎调用收敛到 `UnityCall` 两层隔离层——引擎 API 在特定时机不可用时
  **降级返回默认值**，而不是把异常抛进游戏主循环打断 mod / 游戏；
- **内置 mod 变为两个**：行为日志（ActivityLogMod）+ **自由相机（FreeCameraMod）**。
  自由相机提供固定俯瞰视角，默认只绑定 **F1** 开关 / **F2** 复位，进游戏不会自动夺走镜头；
- **版本号**：加载器 / SDK `2.0.0 → 2.1.0`，AstralParty.Toys `0.3.0 → 0.4.0`。

### 🔧 内置 mod 不再写死为单个

- `ModManager` 的内置 mod 由「单个示例 mod」泛化为**内置 mod 列表**（`BuiltInModIds`），
  安装时逐个写入 `mods\{ModId}\{ModId}.dll` + sidecar，以后新增/移除内置 mod 不需要改安装逻辑；
- 从发布包安装（`InstallPackage`）改为**包内有什么 mod 就装什么**，并按清单逐个校验 SHA256；
- 安装/更新时同名内置 mod 文件会被覆盖为随包版本，升级工具即一并升级内置 mod；
- 模组页勾选项文案更新为「同时安装内置 mod（行为日志 + 自由相机，含 sidecar 元数据）」。

### 🔒 安全

- `InstallPackage` 解包前校验条目相对路径（不含 `..`、非绝对路径），
  避免构造过的发布包把文件写到 `mods\` 之外。

### 🐛 修复

- **CI 打 tag 会取不到内置 mod**：`release.yml` 仍按旧扁平路径 `mods/ActivityLogMod.dll` 取值，
  而发布包已是「每 mod 一个文件夹」，导致 Release 构建直接失败。现在按 `mods/{ModId}/` 目录同步，包内 mod 全部带上。
- 内置 `doorstop_config.json` 控制台默认改为**不置顶**（`consoleTopmost: false`），避免控制台窗口一直压住游戏。

### ♻️ 顺带补齐 v0.3.0 更新日志（当时发布漏写）

- 变速改为**加载器内置能力**（`doorstop_config.json` 的 `speedhackBaseSpeed`），模组页开关直接控制；
  加载器与独立变速器共用 `version.dll`，安装时互相检测并提示；
- mod 能力声明化：取消权限门控，声明「操作游戏」的 mod 仅显示来源警告；
- 修 mod 页进入即自动弹窗、配置被安装/更新重置、加载器设置保存失效等问题；
- 内嵌加载器从 `winmm.dll`（旧代理）改为 `version.dll`（Doorstop 式布局）；
- mod 目录改为「每 mod 一个文件夹」（`mods\{ModId}\{ModId}.dll`），旧扁平布局仍兼容读取。

## [v0.2.0] - 2026-09-11

### ✨ 新增：商店记录时间线（与筹码时间线并列）

回放页面新增「商店记录」标签页，与「筹码记录」并列，展示整局所有玩家的卡牌商店进店与购买情况：

- **候选卡牌**：服务器广播的商店在售卡牌清单（PVE 商店 / PVP 商店），与进店时游戏画面完全一致；
- **购买结果**：结合服务器购买回执（`BuyCards[]` 槽位下标）把「买了几号位、买了哪张卡」翻译成可读文本；
- **已售出**：逐槽位标注该格卡牌已被买走（对应服务器 `Alreadys[]`）；
- **售价与折扣**：单价与折后价（PVE 折扣场景，如「3（折后 2）」）；
- **去重口径**：同一候选组被服务器重复广播时按 `Action.Sn` 与候选内容合并为一次进店，不重复计数；
- **筛选与导出**：支持按玩家 / 商店类型 / 卡牌名筛选，一键导出 CSV（含 BOM，Excel 直接打开不乱码）。

同时补全了商店所需的两张配表（`Card.bin` / `STRCard.bin`），原生 WPF 回退界面也同步增加了「商店记录」页签。

### 🐛 修复：商店「代付」字段误读

此前将 `PVEShopBuyS2C.AssistPlayer` 当作「队友代付买牌」的玩家 ID 展示，经查证为误读：

- `AssistGold` 是 ATM 转账金额（实测恒为 5 星币），`AssistPlayer` 是转账关联标识（回执中为操作关联的大数字，非玩家 ID）；
- PVE 商店唯一的转账入口是 ATM（一次转账 5 星币），商店购买请求的 `assistPlayer` 参数恒为 0；
- 已删除「代付」列，售罄信息以「已售出」逐槽位正确呈现，相关协议文档同步更正。

### 🔧 其他

- 回放解析新增商店协议登记：Action `5029 / 5215 / 5323`，回执 `5030 / 5216 / 5324`；
- 真实回放测试扩为 52 项，覆盖商店进店 / 购买 / 去重 / 售罄合成用例，并通过 10 局真实回放数据验证。

---

## [v0.1.0] - 2026-09-11

### ✨ 新增

- 应用图标（Windows 资源管理器 / 任务栏显示）。
